function [state_dot] =  rocketKinetics(~,state,w_rw,t_ext)
%%% Rigid body rotational dynamics implementation
% Rishav (2020/6/14)

satelliteParams;  % Import satellite parameters

w = state; % Angular velocities of satellite
S = [0 -w(3) w(2); w(3) 0 -w(1); -w(2) w(1) 0]; % Cross product operation

% Rocket rotational dynamics
state_dot =  - J_sat\(cross(w,J_sat*w) - t_ext);
end